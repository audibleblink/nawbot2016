const imageSearch = require('../dist/lib/image_search')
const chai        = require('chai')
const expect      = chai.expect
const sinon       = require("sinon")
const sinonChai   = require("sinon-chai")
chai.use(sinonChai)


describe("imageSearch", function() {
  
  xit("fetches an image url", function() {
  })
  
  xit("returns when encountering an error", function() {
  })
  
})
